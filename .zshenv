#!/bin/zsh

# Add rbenv/pyenv to PATH for scripting. Make sure this is the last PATH variable change.
export PATH="$HOME/.rbenv/bin:$PATH"
export PATH="$HOME/.pyenv/bin:$PATH"

echo 'Sourced .zshenv'
